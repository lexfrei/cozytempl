package fragment

import (
	"github.com/lexfrei/cozytempl/internal/k8s"
	"github.com/lexfrei/cozytempl/internal/view/page"
)

// AppTableFragment renders just the tbody rows for htmx partial swap.
templ AppTableFragment(tenant string, apps []k8s.Application) {
	@page.AppTableRows(tenant, apps)
}
